# Интеграция AI для автоматического решения тестов

## Описание

Система поддерживает автоматический поиск ответов на вопросы с использованием AI (OpenAI GPT, Google Gemini и др.).

## Возможности

- ✅ Автоматический поиск ответов для всех типов вопросов:
  - **Radio** (одиночный выбор)
  - **Checkbox** (множественный выбор)
  - **Drag and Drop** (сопоставление)
  - **Code** (написание кода)
  - **Text** (текстовый ответ)

- ✅ Поддержка нескольких AI провайдеров:
  - **DeepSeek** (рекомендуется - дешево и качественно)
  - OpenAI (GPT-3.5, GPT-4)
  - Google Gemini
  - Возможность добавления других

## Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Получите API ключ

#### DeepSeek (рекомендуется - дешево и качественно) ⭐
1. Зарегистрируйтесь на https://platform.deepseek.com/
2. Пополните баланс (от $5, очень дешево)
3. Создайте API ключ в разделе API Keys
4. Скопируйте ключ

**Преимущества DeepSeek:**
- 💰 Очень дешево: ~$0.14 за 1M токенов (в 10-100 раз дешевле OpenAI)
- 🎯 Высокая точность для образовательных задач
- 🚀 Быстрая работа
- 🔧 Совместим с OpenAI API (легко переключаться)

#### OpenAI
1. Зарегистрируйтесь на https://platform.openai.com/
2. Создайте API ключ в разделе API Keys
3. Скопируйте ключ

#### Google Gemini (бесплатная альтернатива)
1. Зарегистрируйтесь на https://makersuite.google.com/
2. Создайте API ключ
3. Скопируйте ключ

### 3. Настройте конфигурацию

Отредактируйте файл `config.json`:

#### Вариант 1: OpenRouter (рекомендуется)
```json
{
  "ai_enabled": true,
  "ai_provider": "openrouter",
  "ai_api_key": "sk-or-v1-ВАШ_КЛЮЧ",
  "ai_model": "deepseek/deepseek-chat",
  "use_ai_for_tests": true,
  "confidence_threshold": 0.7,
  "fallback_to_manual": true
}
```

**Доступные модели OpenRouter:**
- `deepseek/deepseek-chat` - очень дешево ($0.14/1M токенов), хорошее качество
- `anthropic/claude-3-haiku` - быстро и качественно ($0.25/1M токенов)
- `meta-llama/llama-3-8b-instruct` - бесплатно, среднее качество
- `openai/gpt-3.5-turbo` - классика ($0.50/1M токенов)
- `openai/gpt-4-turbo` - лучшее качество ($10/1M токенов)

#### Вариант 2: OpenAI напрямую
```json
{
  "ai_enabled": true,
  "ai_provider": "openai",
  "ai_api_key": "sk-ВАШ_КЛЮЧ",
  "ai_model": "gpt-3.5-turbo",
  "use_ai_for_tests": true,
  "confidence_threshold": 0.7,
  "fallback_to_manual": true
}
```

#### Вариант 3: DeepSeek напрямую
```json
{
  "ai_enabled": true,
  "ai_provider": "deepseek",
  "ai_api_key": "ВАШ_КЛЮЧ",
  "ai_model": "deepseek-chat",
  "use_ai_for_tests": true,
  "confidence_threshold": 0.7,
  "fallback_to_manual": true
}
```

**Параметры:**
- `ai_enabled` - включить/выключить AI (true/false)
- `ai_provider` - провайдер AI ("openrouter", "openai", "deepseek", "google")
- `ai_api_key` - ваш API ключ
- `ai_model` - модель AI (зависит от провайдера)
- `use_ai_for_tests` - использовать AI для промежуточных тестов
- `confidence_threshold` - порог уверенности (0.0-1.0)
- `fallback_to_manual` - переключаться на ручной режим при ошибках

## Использование

### Автоматический режим

Просто запустите скрипт как обычно. Если AI включен, он автоматически будет искать ответы:

```bash
python help.py
```

### Программное использование

```python
from ai_helper import AIHelper

# Инициализация
ai = AIHelper(api_key="your-key", provider="openai")

# Одиночный выбор
question = "Какой язык используется для веб-разработки?"
options = ["Python", "JavaScript", "C++"]
answer = ai.find_answer(question, options, "radio")
print(f"Ответ: {answer}")

# Множественный выбор
question = "Выберите языки программирования:"
options = ["Python", "HTML", "JavaScript", "CSS"]
answers = ai.find_answer(question, options, "checkbox")
print(f"Ответы: {answers}")

# Drag and Drop
question = "Сопоставьте термины с определениями"
options = {
    "targets": ["Язык разметки", "Язык программирования"],
    "draggables": ["HTML", "Python", "CSS", "JavaScript"]
}
mapping = ai.find_answer(question, options, "drag_and_drop")
print(f"Сопоставления: {mapping}")

# Код
question = "Напишите функцию для сложения двух чисел"
code = ai.find_answer(question, [], "code")
print(f"Код:\n{code}")

# Текст
question = "Сколько байт в килобайте?"
answer = ai.find_answer(question, [], "text")
print(f"Ответ: {answer}")
```

## Интеграция с существующим кодом

Для интеграции AI в существующий скрипт `help.py`, добавьте следующий код:

```python
# В начале файла
from ai_helper import AIHelper
import json

# Загрузка конфигурации
def load_config():
    try:
        with open('config.json', 'r', encoding='utf-8') as f:
            return json.load(f)
    except:
        return {"ai_enabled": False}

# В функции main()
config = load_config()
ai_helper = None

if config.get("ai_enabled"):
    ai_helper = AIHelper(
        api_key=config.get("ai_api_key"),
        provider=config.get("ai_provider", "openai")
    )

# При обработке вопроса
if ai_helper and ai_helper.enabled:
    # Извлекаем вопрос и варианты из страницы
    question_text = driver.find_element(...).text
    options = [opt.text for opt in driver.find_elements(...)]
    
    # Получаем ответ от AI
    answer = ai_helper.find_answer(question_text, options, task_type)
    
    # Используем ответ для выбора в форме
    if answer:
        # Ваш код для выбора ответа
        pass
```

## Стоимость использования

### OpenRouter (рекомендуется)
**DeepSeek через OpenRouter:**
- $0.14 за 1 миллион токенов входных
- $0.28 за 1 миллион токенов выходных
- ~$0.001-0.002 за 10 вопросов
- **Самый дешевый вариант!**

**Claude 3 Haiku через OpenRouter:**
- $0.25 за 1M токенов
- ~$0.002-0.003 за 10 вопросов
- Хорошее качество

**Llama 3 через OpenRouter:**
- Бесплатно (с лимитами)
- Среднее качество

### OpenAI напрямую
**GPT-3.5-turbo:**
- $0.50 за 1M токенов входных
- $1.50 за 1M токенов выходных
- ~$0.005-0.01 за 10 вопросов

**GPT-4-turbo:**
- $10 за 1M токенов входных
- $30 за 1M токенов выходных
- ~$0.10-0.20 за 10 вопросов
- Лучшее качество

### DeepSeek напрямую
- $0.14 за 1M токенов
- ~$0.001-0.002 за 10 вопросов
- Требует отдельной регистрации

### Google Gemini
- Бесплатно до 60 запросов в минуту
- Хорошо для тестирования
- Может быть медленнее

**Рекомендация:** Используйте OpenRouter с моделью `deepseek/deepseek-chat` для лучшего соотношения цена/качество.

## Безопасность

⚠️ **ВАЖНО:**
- Не коммитьте `config.json` с API ключом в Git
- Добавьте `config.json` в `.gitignore`
- Храните API ключи в безопасном месте
- Используйте переменные окружения для продакшена

## Ограничения

- AI может ошибаться (точность ~70-90%)
- Требуется интернет-соединение
- Есть лимиты на количество запросов
- Стоимость использования API

## Решение проблем

### AI не работает
1. Проверьте API ключ в `config.json`
2. Убедитесь, что `ai_enabled: true`
3. Проверьте интернет-соединение
4. Проверьте баланс API аккаунта

### Низкая точность ответов
1. Попробуйте другую модель (GPT-4 точнее GPT-3.5)
2. Увеличьте `confidence_threshold`
3. Включите `fallback_to_manual`

### Ошибки импорта
```bash
pip install --upgrade openai google-generativeai
```

## Примеры

См. файл `ai_helper.py` для примеров использования.

## Поддержка

Для вопросов и предложений создайте issue в репозитории.
